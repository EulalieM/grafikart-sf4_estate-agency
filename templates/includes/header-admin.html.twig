<header class="bg-dark">
    <nav class="container navbar navbar-expand-lg navbar-light">
        <div class="container-fluid">
            <a class="navbar-brand text-white" href="{{ path('ADMIN') }}">Administration</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                    <li class="nav-item">
                        <a class="nav-link active text-white" aria-current="page" href="{{ path('ADMIN_PROPERTY_INDEX') }}">Propriétés</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link text-white" href="{{ path('ADMIN_SPECIFICATION_INDEX') }}">Spécifications</a>
                    </li>
                </ul>
            </div>
            <a href="{{ path('HOMEPAGE') }}" class="btn btn-light btn-sm me-3">Retour au site</a>
            <a class="btn btn-danger btn-sm" href="{{ path('app_logout') }}">Déconnexion</a>
        </div>
    </nav>
</header>